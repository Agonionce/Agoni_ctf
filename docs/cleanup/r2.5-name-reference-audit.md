# R2.5 Name and Legacy Reference Audit

Audit date: 2026-08-20

The pre-cleanup scan covered source, documentation, prompts, metadata, and
tracked local configuration while excluding `.git/`, `.venv/`, `config.json`,
runtime logs, checkpoints, caches, and binary image content.

## Findings before remediation

| File | Lines | Legacy content | Planned treatment |
|---|---:|---|---|
| `README.md` | 4, 13, 16, 18, 38–39, 122 | legacy project name, author name, and repository links | Full rewrite as Agonionce README |
| `AGENTS.md` | 33, 38, 64, 71, 82 | legacy project name and monolithic solver architecture | Full rewrite for R0–R2 |
| `.claude/settings.local.json` | 7–31 | prior-author machine paths and legacy repository directory | Remove machine-local metadata |
| `docs/architecture/upstream-baseline.md` | 3–13 | upstream project identity and migration framing | Remove historical identity document |
| `docs/architecture/r0-core-runtime.md` | 24, 51, 67–95 | retired legacy boundary and old agent files | Rewrite section to current boundary |
| `docs/cli_ui_redesign.md` | multiple | retired solver and legacy CLI examples | Remove superseded design |
| `docs/inputer_and_submiter.md` | 9, 146–230 | platform-specific legacy adapter examples | Replace with current platform-adapter guide |
| `docs/multi_agent_design.md` | multiple | legacy project identity, retired solver, retired tree | Remove out-of-scope historical design |
| `docs/ssh_shell_to_local_bash_design.md` | multiple | retired solver and retired ctf_tool path | Remove superseded design |
| `agent/<retired-solver>.py` | whole file | retired monolithic solver implementation and role alias | Remove after reference closure check |
| `agent/analyzer.py`, `agent/memory.py` | multiple | retired agent chain and role alias | Remove with retired implementation cluster |
| `utils/tools.py`, `ctf_tool/`, `prompt.yaml` | whole path | retired BaseTool loader and prompt system | Remove after dependency check |
| `utils/llm_request.py`, `utils/text.py` | selected lines | retired solver role alias | Rename active role to planner; remove unused helper |
| `1.png`, `qq_group.jpg`, `tch_logo.png` | binary | old name/logo, community QR, event branding | Remove branding assets |

## Reference-closure result

- The retired monolithic-solver cluster is not imported by `main.py`, `Workflow`,
  R0 Runtime, R1 ToolRuntime, R2 Intelligence, or the test suite.
- `ctf_tool/`, `utils/tools.py`, and `prompt.yaml` are referenced only by that
  retired cluster and superseded historical documents.
- `agent/checkpoint.py` remains actively imported by CLI checkpoint and resume
  paths and must be preserved.
- `utils/llm_request.py` and `utils/text.py::optimize_text` remain active and
  require targeted cleanup rather than deletion.

## Final verification target

After remediation, an exact case-insensitive repository scan—excluding this
historical audit's quoted evidence, Git history, ignored user secrets, logs,
checkpoints, caches, and binary assets—must return no legacy project-name,
author-name, repository-link, or retired-solver references. This audit will then
be redacted to generic legacy labels so the final documentation also conforms
to the Agonionce identity rule.

## Final verification result

The audit evidence above has been redacted to generic legacy labels after
remediation. A case-insensitive scan of the complete tracked and untracked
working tree, with only Git internals, ignored user configuration, runtime
logs/checkpoints, virtual environments, caches, and bytecode excluded, found:

```text
legacy project-name references: 0
prior-author references: 0
legacy repository-link references: 0
retired solver class/role references: 0
```
